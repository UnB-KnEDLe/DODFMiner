version: '3.4'
services:
  dodfminer:
    build:
      context: .
      network: host
    network_mode: host
    entrypoint:
      - python3
      - dodfminer
    volumes:
      - .:/home/App
      - ${DATA_PATH}:/home/App/data/
